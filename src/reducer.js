export const initialState = {
  basket: [],
}

function reducer(state, action) {
  switch (action) {
    case 'ADD_TO_BASKET':
      break

    case 'REMOVE_TO_BASKET':
      break

    default:
      return state
  }
}
